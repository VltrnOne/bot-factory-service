{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": { "name": "" },
  "spec": {
    "replicas": 1,
    "template": {
      "spec": {
        "containers": [
          {
            "name": "bot-agent",
            "image": "my-registry/bot-agent:latest",
            "env": []
          }
        ]
      }
    }
  }
}
